// Vitest setup file: registers `@testing-library/jest-dom` matchers
// (`toBeInTheDocument`, `toBeDisabled`, ...) onto the vitest `expect` via the
// library's auto-extend. Loaded once per vitest run via `setupFiles` in
// vitest.config.ts.
import "@testing-library/jest-dom/vitest";
