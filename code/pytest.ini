[pytest]
testpaths = tests
addopts = --ignore=tests/e2e
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    gpu: marks tests requiring CUDA GPU
    network: marks tests requiring network access (AnyText2 server, translation API)
