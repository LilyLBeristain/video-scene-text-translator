[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = ..
asyncio_mode = auto
# Skip integration tests by default — opt in with `-m gpu`.
addopts = -m "not gpu"
markers =
    gpu: Integration tests that require a GPU + live AnyText2 server. Skipped by default.
